spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  application:
    name: Board

  # 🚀 DB 연결 수정 (핵심!)
  datasource:
    # Railway가 제공하는 변수 이름과 일치시키고, 변수 치환 오류를 우회하기 위해
    # Railway가 자동으로 제공하는 개별 변수 이름을 사용하거나, 
    # 혹은 Railway의 변수 이름을 그대로 사용하도록 변경합니다.
    # 이전의 ${DB_URL}이 아닌, Railway에서 생성된 변수 이름인 ${MYSQL_URL}로 시도합니다.
    url: ${MYSQL_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}

  jpa:
    # ⚠️ 경고를 피하기 위해 database-platform을 삭제합니다.
    # database-platform: org.hibernate.dialect.MySQLDialect 
    properties:
      hibernate:
        show_sql: true # '[show_sql]' 대신 일반 표기 사용
        format_sql: true # '[format_sql]' 대신 일반 표기 사용
        jdbc:
          time_zone: Asia/Seoul
    hibernate:
      ddl-auto: update

  jackson:
    serialization:
      write-dates-as-timestamps: false
      fail-on-empty-beans: false
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non-null
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Seoul

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

# Swagger 설정 수정 (변동 없음)
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true
  api-docs:
    path: /v3/api-docs
  packages-to-scan:
    - com.springboot.board.api.v1.controller
    - com.springboot.board.web.controller
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

app:
  base-url: https://korea-sky-planner.com
  upload-dir: uploads